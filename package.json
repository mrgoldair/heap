{
  "name": "@mrgoldair/heap",
  "version": "0.4.12",
  "description": "An implementation of a heap data structure",
  "homepage": "https://github.com/mrgoldair/heap#readme",
  "main": "lib/Heap.js",
  "types": "lib/Heap.d.ts",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.json"
      }
    },
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "testMatch": [
      "**/specs/*.+(ts|tsx|js)"
    ]
  },
  "devDependencies": {
    "@types/jest": "26.0.4",
    "fast-check": "^2.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5"
  }
}
